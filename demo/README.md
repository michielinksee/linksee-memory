# notekeeper

A tiny note CLI. **One command setup.**

## Commands

| Command | What it does |
|---|---|
| `notekeeper --init` | create the notes database |
| `notekeeper --list` | list your notes |
| `notekeeper --export` | export all notes to Markdown |
